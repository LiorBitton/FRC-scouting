import 'package:flutter/cupertino.dart';

class GameData extends StatelessWidget {
  const GameData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
